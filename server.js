if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
 
  // Accounts.createUser({ 
  //  username: "user",
  //  email: "me@brandontruong.com",
  //  password: "password"
  // });

}
